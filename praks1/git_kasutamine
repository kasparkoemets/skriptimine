* root kasutajana:
installi - apt-get install git
* tavakasutajana:
esmakordselt:
a) seadista - määra kasutajanimi, email, editor
git config --global user.name "Kaspar Koemets"
git config --global user.email kaspar.koemets@khk.ee
git config --global core.editor nano

b)loo kataloog, kus hakkad skriptima
mkdir skriptimine

c) sisene antud kausta
cd skriptimine

d) tekita lokaalne repositoorium git süsteemi jaoks antud kaustas
git init

e) git remote add nimi adress
git remote add skriptimine hhtps://github.com/kasparkoemets/reponimi.git

f) git remote kontroll
git remote -v

põhikasutamine:

g)pane fail järgmisele:
git add failinimi

h)lisa kirjelduse muudatuse kohta
git commit -m "lühikirjelduse lause"

i) lisa kaugrepositooriumi
git push -uf skriptimine master
